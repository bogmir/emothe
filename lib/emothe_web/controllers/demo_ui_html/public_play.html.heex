<div class="play-text-page min-h-screen">
  <div class="max-w-7xl mx-auto px-4 py-6 lg:grid lg:grid-cols-[16rem_minmax(0,1fr)] lg:gap-6">
    <aside id="play-sections-panel" class="mb-4 lg:mb-0 lg:sticky lg:top-4 lg:self-start">
      <div class="rounded-box border border-base-300 bg-base-100/90 backdrop-blur-sm shadow-sm">
        <button
          type="button"
          class="flex w-full items-center justify-between gap-2 px-3 py-2.5 text-sm font-semibold text-primary cursor-pointer"
        >
          <span class="flex items-center gap-2">
            <.icon name="hero-list-bullet-micro" class="size-4" /> Contents
          </span>
          <.icon name="hero-chevron-up-micro" class="size-4 text-base-content/40" />
        </button>

        <div class="border-t border-base-300 max-h-[65vh] overflow-y-auto px-2 py-2 space-y-3">
          <section>
            <h3 class="px-2 pt-1 text-[10px] font-semibold uppercase tracking-widest text-base-content/40">
              Metadata
            </h3>
            <nav class="mt-1 space-y-px">
              <a
                href="#meta-overview"
                class="block rounded-md px-2 py-1 text-xs text-base-content/70 transition-colors hover:bg-primary/10 hover:text-primary active:bg-primary/20"
              >
                Overview
              </a>
              <a
                href="#meta-editors"
                class="block rounded-md px-2 py-1 text-xs text-base-content/70 transition-colors hover:bg-primary/10 hover:text-primary active:bg-primary/20"
              >
                Editors
              </a>
            </nav>
          </section>

          <section>
            <h3 class="px-2 pt-1 text-[10px] font-semibold uppercase tracking-widest text-base-content/40">
              Sections
            </h3>
            <nav class="mt-1 space-y-px">
              <a
                href="#sec-acto-1"
                class="block rounded-md py-1 text-xs text-base-content/70 transition-colors hover:bg-primary/10 hover:text-primary active:bg-primary/20"
                style="padding-left: 0.5rem"
              >
                Acto I
              </a>
              <a
                href="#sec-escena-1"
                class="block rounded-md py-1 text-xs text-base-content/70 transition-colors hover:bg-primary/10 hover:text-primary active:bg-primary/20"
                style="padding-left: 1.125rem"
              >
                Escena I
              </a>
            </nav>
          </section>
        </div>
      </div>

      <a
        href="/demo/ui/admin/play"
        class="mt-2 flex items-center gap-1.5 rounded-box border border-base-300 bg-base-100/90 px-3 py-2 text-xs text-base-content/60 hover:text-primary hover:border-primary/30 transition-colors"
      >
        <.icon name="hero-pencil-square-micro" class="size-3.5" /> Edit in Admin
      </a>
    </aside>

    <div>
      <header
        id="meta-overview"
        class="play-header mb-8 border-b border-base-300 pb-6 scroll-mt-20 text-center"
      >
        <h2 class="play-author">{@play.author_name}</h2>
        <h1 class="play-title font-bold">{@play.title}</h1>

        <div id="meta-sources" class="scroll-mt-20">
          <div :for={source <- @play.sources} class="mt-4 text-xs text-base-content/50">
            <p :if={source.note} class="italic">{source.note}</p>
          </div>
        </div>

        <div id="meta-editors" class="mt-3 flex flex-wrap justify-center gap-2 scroll-mt-20">
          <span :for={editor <- @play.editors} class="text-xs text-base-content/50">
            {editor.person_name}
            <span class="text-base-content/35">({editor.role})</span>
          </span>
        </div>

        <p :if={@play.verse_count} class="mt-2 text-xs text-base-content/50">
          {if @play.is_verse, do: "#{@play.verse_count} verses", else: "Prose"}
        </p>
      </header>

      <div
        :for={{note, index} <- Enum.with_index(@play.editorial_notes, 1)}
        id={"meta-note-#{index}"}
        class="mb-6 max-w-2xl mx-auto scroll-mt-20 text-justify text-sm"
      >
        <h3 :if={note.heading} class="font-bold text-center mb-2">{note.heading}</h3>
        <div class="whitespace-pre-line">{note.content}</div>
      </div>

      <nav id="play-tab-nav" class="flex border-b border-base-300 mb-6">
        <button
          type="button"
          class="px-4 py-2.5 text-sm font-medium border-b-2 -mb-px transition-colors cursor-pointer border-primary text-primary"
        >
          Text
        </button>
        <button
          type="button"
          class="px-4 py-2.5 text-sm font-medium border-b-2 -mb-px transition-colors cursor-pointer border-transparent text-base-content/50 hover:text-primary/70"
        >
          Characters
        </button>
        <button
          type="button"
          class="px-4 py-2.5 text-sm font-medium border-b-2 -mb-px transition-colors cursor-pointer border-transparent text-base-content/50 hover:text-primary/70"
        >
          Statistics
        </button>
      </nav>

      <div id="play-tab-text">
        <div class="flex gap-4 mb-6 p-2.5 bg-base-200/50 rounded-box border border-base-300">
          <label class="flex items-center gap-2 text-xs cursor-pointer text-base-content/70">
            <input type="checkbox" checked class="checkbox checkbox-xs checkbox-primary" />
            Line numbers
          </label>
          <label class="flex items-center gap-2 text-xs cursor-pointer text-base-content/70">
            <input type="checkbox" checked class="checkbox checkbox-xs checkbox-primary" />
            Stage directions
          </label>
        </div>

        <div class="rounded-box border border-base-300 bg-base-100 p-5 shadow-sm">
          <h2 id="sec-acto-1" class="text-lg font-semibold text-base-content mb-2">Acto I</h2>
          <h3 id="sec-escena-1" class="text-sm font-semibold text-base-content/80 mb-4">
            Escena I
          </h3>

          <div class="space-y-4 text-sm leading-relaxed">
            <div>
              <p class="font-semibold text-base-content">FINEA</p>
              <p class="text-base-content/80">
                <span class="text-base-content/40 font-mono text-xs">1</span>
                Si el saber no me da vida,
              </p>
              <p class="text-base-content/80">
                <span class="text-base-content/40 font-mono text-xs">2</span>
                ¿por qué me lo dais, señores?
              </p>
            </div>

            <div class="italic text-base-content/60">
              <span class="text-base-content/40 font-mono text-xs">[SD]</span>
              (Sale Nise con papeles.)
            </div>

            <div>
              <p class="font-semibold text-base-content">NISE</p>
              <p class="text-base-content/80">
                <span class="text-base-content/40 font-mono text-xs">3</span>
                Llegad, que el tiempo se pasa.
              </p>
            </div>
          </div>
        </div>
      </div>

      <div class="mt-8">
        <h2 class="text-lg font-bold mb-4 text-base-content">Play Statistics (Preview)</h2>
        <.stats_panel statistic={@statistic} play={@play} />
      </div>

      <div class="mt-8">
        <h2 class="text-lg font-bold mb-4 text-base-content">Dramatis Personae (Preview)</h2>
        <div class="grid gap-2 max-w-2xl">
          <div
            :for={char <- Enum.filter(@characters, &(!&1.is_hidden))}
            class="flex items-center gap-3 p-3 bg-base-100/60 border border-base-300 rounded-box"
          >
            <span class="font-semibold text-base-content">{char.name}</span>
            <span :if={char.description} class="text-sm text-base-content/50">
              {char.description}
            </span>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
